import React from 'react';
import ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
  console.log('Spectrum is GREEN');
  ReactDOM.render(
    <h2>Ey, how ya doin?</h2>,
    document.getElementById('root'),
  );
});